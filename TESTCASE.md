# TESTCASE

## 目的

`friend-sync-app` のローカル確認用テストケースをまとめる。  
主に以下を確認する。

- 新規登録 / ログイン
- 設定画面
- 友達画面
- カレンダー画面
- ホーム画面
- 通知画面
- User / Event / Invite / Friend のデータ紐づき

---

## 前提条件

- backend が起動している
- frontend が起動している
- Supabase の `User`, `Friend`, `events`, `invites` テーブルが利用可能
- `User` テーブルには少なくとも以下カラムがある
  - `id`
  - `auth_user_id`
  - `name`
  - `user_id`
  - `note`
  - `mailadress`
  - `icon_url`
  - `plan_status`
- `Friend`
 - `id`
 - `owner_user_id`
 - `friend_user_id`
-`Events`
 - `id`
 - `user_id`
 - `date`
 - `title`
 - `start_time`
 - `end_time`
 - `category`
 - `memo`


---

## 1. 新規登録

### 1-1. 新規登録成功

**手順**

1. アプリを開く
2. 新規登録画面で以下を入力する
   - ユーザ名
   - メールアドレス
   - パスワード
   - パスワード確認
3. パスワード2回が一致した状態で登録する

**期待結果**

- 登録が成功する
- ログイン状態になる
- `User` テーブルに新規ユーザーが作成される
- `User.name` に登録したユーザ名が入る
- `User.user_id` に公開用ユーザIDが自動生成される
- `User.mailadress` に登録したメールアドレスが入る
- `User.auth_user_id` に Supabase Auth のユーザーIDが入る
- `User.plan_status` は初期値 `free`

### 1-2. 新規登録失敗: パスワード不一致

**手順**

1. 新規登録画面でパスワードと確認用パスワードを違う値で入力する
2. 登録する

**期待結果**

- 登録できない
- 不一致であることが画面上で分かる

---

## 2. ログイン / 再ログイン

### 2-1. ログイン成功

**手順**

1. 既存アカウントでログインする

**期待結果**

- ログインできる
- 各画面でログイン中ユーザーの情報が表示される

### 2-2. 再ログイン

**手順**

1. 設定画面で `サインアウト` を押す
2. ログイン画面へ戻る
3. 再度ログインする

**期待結果**

- セッションが切り替わる
- 以後の友達、予定、通知は再ログイン後のユーザー基準で表示される

---

## 3. 設定画面

### 3-1. 初期表示

**確認項目**

- ユーザアイコンが表示される
- ユーザ名に `User.name` が表示される
- ユーザIDに `User.user_id` が表示される
- 一言に `User.note` が表示される
- メールアドレスに `User.mailadress` が表示される
- プランに `User.plan_status` が表示される

### 3-2. プロフィール更新

**手順**

1. 設定画面で以下を変更する
   - ユーザ名
   - ユーザID
   - 一言
2. `プロフィールを更新` を押す

**期待結果**

- 更新成功メッセージが出る
- `User.name` が更新される
- `User.user_id` が更新される
- `User.note` が更新される
- 設定画面の表示も更新後の内容になる
- 友達画面で自分の表示にも反映される

### 3-3. ユーザID重複

**手順**

1. 他ユーザーが使っている `User.user_id` を入力する
2. `プロフィールを更新` を押す

**期待結果**

- 更新できない
- 重複が分かる

### 3-4. アイコン更新

**手順**

1. `画像を選ぶ` を押す
2. 画像を選択する
3. `プロフィールを更新` を押す

**期待結果**

- 画像がアップロードされる
- `User.icon_url` が更新される
- 設定画面のアイコン表示が更新される

### 3-5. メールアドレス更新

**手順**

1. メールアドレスを変更する
2. `アカウントを更新` を押す

**期待結果**

- 更新成功する
- 設定画面のメールアドレス表示が更新される
- `User.mailadress` も更新される

### 3-6. パスワード更新

**手順**

1. 新しいパスワードを入力する
2. 確認用パスワードに同じ値を入力する
3. `アカウントを更新` を押す

**期待結果**

- パスワード更新が成功する

### 3-7. パスワード更新失敗: 確認不一致

**手順**

1. 新しいパスワードと確認用パスワードに違う値を入力する
2. `アカウントを更新` を押す

**期待結果**

- 更新できない
- 一致していないことが分かる

---

## 4. 友達画面

### 4-1. 新規登録直後の状態

**手順**

1. 新規登録したばかりのユーザーで友達画面を開く

**期待結果**

- 上部に自分の情報が表示される
  - 登録したユーザ名
  - 登録時または更新後の `User.user_id`
- 友達一覧は空

### 4-2. 友達検索: User.user_id

**手順**

1. `IDで追加` を押す
2. 相手の `User.user_id` を入力する
3. 検索する

**期待結果**

- 一致するユーザーが見つかる
- 見つかったユーザーの
  - 名前
  - ID
  - 一言
  が表示される

### 4-3. 友達追加

**手順**

1. 検索結果から `追加` を押す

**期待結果**

- 友達一覧に追加される
- `Friend.owner_user_id` はログイン中ユーザーの `User.id`
- `Friend.friend_user_id` は追加した相手の `User.id`

### 4-4. 自分自身は追加できない

**手順**

1. 自分の `User.user_id` を入力して検索 / 追加する

**期待結果**

- 自分は追加できない

### 4-5. 重複追加できない

**手順**

1. すでに追加済みの相手を再度追加する

**期待結果**

- 重複追加できない

### 4-6. QR追加

**手順**

1. 自分のQRを表示する
2. 別アカウントで読み取る

**期待結果**

- `User.user_id` をもとに相手を特定できる
- 友達追加できる

---

## 5. カレンダー画面

### 5-1. 年をまたいで閲覧できる

**手順**

1. カレンダー画面を開く
2. `前年` / `翌年` を操作する

**期待結果**

- 年をまたいでカレンダーを閲覧できる
- 1年固定ではない

### 5-2. 予定追加

**手順**

1. 任意の日付を選ぶ
2. `予定追加` を押す
3. 予定名、日付、開始、終了、色を入れる
4. 保存する

**期待結果**

- 予定が保存される
- `events.user_id` にはログイン中ユーザーの `User.id` が入る
- 保存した日付に予定が表示される

### 5-3. 予定編集

**手順**

1. 既存予定の `編集` を押す
2. 日付、開始時刻、終了時刻、タイトルなどを変更する
3. 更新する

**期待結果**

- 更新成功する
- 日付ピッカーは年 / 月 / 日で選べる
- 時刻ピッカーは開始 / 終了を選べる

### 5-4. ピッカーの確定

**手順**

1. 日付ピッカーを回す
2. `決定` を押す
3. 時刻ピッカーを回す
4. `決定` を押す

**期待結果**

- `決定` を押した内容だけが反映される
- `閉じる` のみでは反映されない

### 5-5. 年ピッカーの挙動

**手順**

1. 日付ピッカーを開く
2. 年のホイールを上下に動かす

**期待結果**

- 操作量以上に年が飛ばない
- スクロールに対して自然に動く

### 5-6. 予定削除

**手順**

1. 既存予定の `削除` を押す

**期待結果**

- 自分の予定だけ削除できる

---

## 6. ホーム画面

### 6-1. 日付選択

**手順**

1. ホーム画面の日付選択を開く
2. 任意の年 / 月 / 日を選ぶ

**期待結果**

- 任意の日付に移動できる
- 1年固定ではない

### 6-2. お誘いボタン

**手順**

1. ある日付で友達を `誘う`

**期待結果**

- その日付では `誘いました` に変わる
- 別の日付に移ると `誘う` のまま

### 6-3. 空き判定

**確認項目**

- 予定の空き / 忙しさ判定がログイン中ユーザーや友達の `User.id` 基準で行われる
- `name` や `user_id` 変更で紐づきが壊れない

---

## 7. 通知画面

### 7-1. 自分宛の通知のみ表示

**前提**

- `invites.to_user_id` は受信者の `User.id`

**手順**

1. 別ユーザーから自分宛てに invite を送る
2. 通知画面を開く

**期待結果**

- `to_user_id == 自分の User.id` の invite だけ表示される
- 他人宛ての invite は表示されない

### 7-2. 招待の送信者 / 受信者の紐づき

**確認項目**

- `invites.from_user_id` は送信者の `User.id`
- `invites.to_user_id` は受信者の `User.id`

---

## 8. データ整合性

### 8-1. User テーブル基準の紐づき

**確認項目**

- 友達は `User.id` で紐づく
- 予定は `events.user_id = User.id`
- 招待は `invites.from_user_id = User.id`
- 招待は `invites.to_user_id = User.id`

### 8-2. 公開ID変更後の整合性

**手順**

1. 設定画面で `User.user_id` を変更する

**期待結果**

- 既存の友達関係は壊れない
- 既存の予定は壊れない
- 既存の通知は壊れない

### 8-3. 名前変更後の整合性

**手順**

1. 設定画面で `User.name` を変更する

**期待結果**

- 既存の友達関係は壊れない
- 既存の予定は壊れない
- 表示名のみ新しいものに変わる

---

## 9. 異常系

### 9-1. backend 未起動

**手順**

1. backend を止めた状態でアプリ操作する

**期待結果**

- 接続エラーが分かる
- アプリが落ちない

### 9-2. セッション切れ

**手順**

1. 古いトークン状態で友達 / 予定 / 通知を開く

**期待結果**

- 403 / 認証エラー時に再ログイン導線で復旧できる

---

## 10. 回帰確認の優先順

時間がない時は、最低限この順番で確認する。

1. 新規登録
2. 設定画面でプロフィール更新
3. 友達追加
4. 予定追加 / 編集 / 削除
5. ホームでお誘い
6. 通知表示
7. 名前 / `user_id` 変更後もデータが壊れないか確認
